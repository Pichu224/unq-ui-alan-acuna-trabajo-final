# Trabajo Final UI
descargar el proyecto con el comando:

git clone https://github.com/Pichu224/unq-ui-alan-acuna-trabajo-final.git

## Instalación
instalar dependencias del proyecto con el comando:

npm install

## Ejecutar
levantar el proyecto con el siguiente comando y entrar al puerto solicitado por consola:

npm run dev