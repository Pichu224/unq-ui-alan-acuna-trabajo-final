export type LeaderboardEntry = {
  name: string;
  score: number;
  wordsCount: number;
  createdAt: number;
};

export type NewLeaderboardEntry = {
  name: string;
  score: number;
  wordsCount: number;
};